pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Testing webhook add in'
                // Your build steps go here
            }

        }
    }
}
